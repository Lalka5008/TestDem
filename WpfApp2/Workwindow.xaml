<Window x:Class="WpfApp2.Workwindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp2"
        mc:Ignorable="d"
        FontFamily="Times New Roman"
        Title="Workwindow" Height="450" Width="800" Icon="/Icon.ico">
    <Grid>
        <TextBlock x:Name="TextBlockFullName" Text="sdadassad" Margin="575,0,0,410" />

        <Grid Background="#FFE5E5E5" Margin="0,54,0,0">
            <ScrollViewer>
                        <Grid>
                            <ItemsControl Margin="0,0,0,150" ItemsSource="{Binding Products}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Height="200">
                                            <Image Source="{Binding ImageFullPath}" HorizontalAlignment="Left" Height="100" Margin="10,10,0,0" VerticalAlignment="Top" Width="100"/>
                                            <TextBlock HorizontalAlignment="Left" Margin="145,10,0,0" TextWrapping="Wrap" Text="{Binding Category.CategoryType}" VerticalAlignment="Top" Width="162"/>
                                            <TextBlock HorizontalAlignment="Left" Margin="342,10,0,0" TextWrapping="Wrap" Text="{Binding Product.ProductName}" VerticalAlignment="Top" Width="175"/>
                                            <TextBlock HorizontalAlignment="Left" Margin="145,31,0,0" TextWrapping="Wrap" Text="{Binding Info}" VerticalAlignment="Top" Width="202"/>
                                            <TextBlock HorizontalAlignment="Left" Margin="145,52,0,0" TextWrapping="Wrap" Text="{Binding Creater.CreaterName}" VerticalAlignment="Top" Width="182"/>
                                            <TextBlock HorizontalAlignment="Left" Margin="145,73,0,0" TextWrapping="Wrap" Text="{Binding Import.ImportName}" VerticalAlignment="Top" Width="162"/>
                                            <TextBlock HorizontalAlignment="Left" Margin="145,94,0,0" TextWrapping="Wrap" Text="{Binding Price, StringFormat=Цена: {0}Р.}" VerticalAlignment="Top" Width="162"/>
                                            <TextBlock HorizontalAlignment="Left" Margin="145,115,0,0" TextWrapping="Wrap" Text="{Binding Unit.UnitName}" VerticalAlignment="Top" Width="162"/>
                                            <TextBlock HorizontalAlignment="Left" Margin="145,136,0,0" TextWrapping="Wrap" Text="{Binding Quantity, StringFormat=Кол-во {0}}" VerticalAlignment="Top" Width="162"/>
                                            <TextBlock HorizontalAlignment="Left" Margin="690,81,0,0" TextWrapping="Wrap" Text="{Binding Sale, StringFormat=Скидка {0}}" VerticalAlignment="Top"/>
                                            <Border BorderBrush="Black" BorderThickness="1"/>
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Grid>
                    </ScrollViewer>
                </Grid>
        <Button Content="Создать заявку" HorizontalAlignment="Left" Margin="10,4,0,0" VerticalAlignment="Top" Click="Button_Click" FontFamily="Times New Roman"/>

    </Grid>
</Window>
